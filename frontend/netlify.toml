[build]
  command = "npm install && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Переменные окружения должны быть настроены в Netlify Dashboard
# Site settings > Environment variables
[build.environment]
  NODE_VERSION = "20"

